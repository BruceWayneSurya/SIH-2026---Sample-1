# Base URL of the Pragyan REST API (backend). In production point this at your
# deployed backend. In local dev the default (http://localhost:3001) is used.
# The SPA proxies /api and /uploads to this URL via next.config rewrites.
API_BASE_URL=http://localhost:3001
